{
  "name": "metafusion-docs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs --port 3001 --host 0.0.0.0",
    "build": "vitepress build docs",
    "check:nav": "node scripts/check-nav.mjs",
    "preview": "vitepress preview docs --port 3001 --host 0.0.0.0",
    "start": "sirv site --port 3001 --host 0.0.0.0 --gzip --brotli"
  },
  "dependencies": {
    "sirv-cli": "^3.0.0"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  },
  "overrides": {
    "vitepress": {
      "vite": "6.4.3",
      "esbuild": "0.25.0"
    }
  }
}
