{
  "name": "metafusion-docs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs --port 3001 --host 0.0.0.0",
    "build": "vitepress build docs",
    "preview": "vitepress preview docs --port 3001 --host 0.0.0.0",
    "start": "sirv docs/.vitepress/dist --port 3001 --host 0.0.0.0 --single --gzip --brotli"
  },
  "dependencies": {
    "sirv-cli": "^3.0.0"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
